import React from 'react'
import PublishButton from './PublishButton'

interface Props {
    
}

const PcSidebar = (props: Props) => {
    return (
        <React.Fragment>
            <PublishButton></PublishButton>
        </React.Fragment>
    )
}

export default PcSidebar
