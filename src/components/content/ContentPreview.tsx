import React from 'react'

interface Props {
    
}

const ContentPreview = (props: Props) => {
    return (
        <React.Fragment>
             
        </React.Fragment>
    )
}

export default ContentPreview